{
  "name": "silvianacmp",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "bootstrap": "^3.3.7"
  },
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "prebuild": "pwd && rm index.html || true && rm -r ./static || true && rm asset-manifest.json || true",
    "build": "react-scripts build",
    "postbuild": "cp -a ./build/ ./",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}